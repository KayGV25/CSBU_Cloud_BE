services:
  postgres:
    container_name: csbu_pg
    image: postgres
    environment:
      POSTGRES_USER: csbu_cloud
      POSTGRES_PASSWORD: csbu_cloud_password
      PGDATA: /var/lib/postgresql/data
      TZ: ${APP_TIMEZONE:-UTC}
    volumes:
      - postgres:/var/lib/postgresql/data
    ports:
      - 5435:5432
    networks:
      - csbu-network
    restart: unless-stopped

networks:
  csbu-network:
    driver: bridge

volumes:
  postgres:
  pgadmin:
  mongo: